const groups = {};

function createGroup(socket, groupName) {
  // Create and manage groups
}

function handleMessage(socket, msg) {
  // Direct message or group message logic
}

module.exports = {
  createGroup,
  handleMessage,
};
